import React from 'react';

const userinput = (props) => {
  return (
    <div>
      <input type="button" />
      <input username="userinputcontent" />
    </div>
  );
};

export default userinput;
